# pyglotaran==0.5.1
# pyglotaran-extras==0.5.0

# Because the course needs not yet released changes, we need to directly install from github
# After the 0.6.0 release this can be changed to a stable releas
git+https://github.com/glotaran/pyglotaran
git+https://github.com/glotaran/pyglotaran-extras 
